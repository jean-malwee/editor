# Configurações do Frontend (Editor)

# Determina se o frontend deve usar o backend API para storage
# true = usar backend API (Google Cloud Storage via backend)
# false = usar localStorage do navegador
VITE_USE_CLOUD_STORAGE=false

# Opcional: URL base do backend (se diferente do padrão)
# VITE_API_BASE_URL=/api

# Configurações de desenvolvimento
# VITE_DEBUG_STORAGE=true
